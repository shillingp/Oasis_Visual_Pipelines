<UserControl
    d:Background="White"
    d:DesignHeight="80"
    d:DesignWidth="180"
    mc:Ignorable="d"
    x:Class="Oasis_Pipelines.Shared.Wpf.MultiSelectControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="clr-namespace:Oasis_Pipelines.Shared.Wpf.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wpf="clr-namespace:Oasis_Pipelines.Shared.Wpf"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <converters:CollectionContainsConverter x:Key="CollectionContainsConverter" />
    </UserControl.Resources>
    <Grid MinHeight="30">
        <TextBlock
            HorizontalAlignment="Center"
            Style="{StaticResource MaterialDesignCharacterCounterTextBlock}"
            Text="No Table Input"
            VerticalAlignment="Center" />
        <ListView
            ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=wpf:MultiSelectControl}, Path=ItemsSource}"
            MaxHeight="100"
            ScrollViewer.CanContentScroll="False"
            SelectionMode="Extended"
            d:ItemsSource="{d:SampleData ItemCount=5}"
            x:Name="ItemSelectionListView">
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource MaterialDesignListBoxItem}" TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Card.Background}" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <CheckBox
                        Checked="CheckBox_Checked"
                        Content="{Binding}"
                        Unchecked="CheckBox_Unchecked"
                        VerticalAlignment="Center">
                        <CheckBox.IsChecked>
                            <MultiBinding Converter="{StaticResource CollectionContainsConverter}" Mode="OneWay">
                                <Binding Path="SelectedItems" RelativeSource="{RelativeSource AncestorType=wpf:MultiSelectControl}" />
                                <Binding Path="." />
                            </MultiBinding>
                        </CheckBox.IsChecked>
                    </CheckBox>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>