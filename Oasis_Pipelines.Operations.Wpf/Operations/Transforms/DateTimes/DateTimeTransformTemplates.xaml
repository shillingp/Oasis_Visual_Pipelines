<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:controls="clr-namespace:Oasis_Pipelines.Controls;assembly=Oasis_Pipelines.Controls"
    xmlns:converters="clr-namespace:Oasis_Pipelines.Operations.Wpf.Converters"
    xmlns:globals="clr-namespace:Oasis_Pipelines.Globals;assembly=Oasis_Pipelines"
    xmlns:local="clr-namespace:Oasis_Pipelines.Operations.Transforms.DateTimes;assembly=Oasis_Pipelines.Operations"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate DataType="{x:Type local:DateTimeOffsetOperation}">
        <DataTemplate.Resources>
            <converters:EnumValuesItemsSourceConverter x:Key="EnumValuesItemsSourceConverter" />
        </DataTemplate.Resources>
        <DockPanel>
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding OperationTitle}"
                TextWrapping="Wrap" />

            <UniformGrid Columns="2">
                <controls:NumericUpDownControl NumberValue="{Binding OffsetValue, Mode=TwoWay}" />
                <ComboBox
                    ItemsSource="{Binding Converter={StaticResource EnumValuesItemsSourceConverter}, ConverterParameter={x:Type globals:TimeDateOffset}}"
                    SelectedItem="{Binding TimeDateOffset}"
                    TextBlock.TextAlignment="Center" />
            </UniformGrid>
        </DockPanel>
    </DataTemplate>
</ResourceDictionary>