<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="clr-namespace:Oasis_Pipelines.Operations.Wpf.Converters"
    xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:strings="clr-namespace:Oasis_Pipelines.Operations.Transforms.Strings;assembly=Oasis_Pipelines.Operations"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate DataType="{x:Type strings:ChangeCaseOperation}">
        <DataTemplate.Resources>
            <converters:IsEqualConverter x:Key="IsEqualConverter" />
        </DataTemplate.Resources>
        <DockPanel>
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding OperationTitle}"
                TextWrapping="Wrap" />

            <StackPanel Orientation="Horizontal">
                <RadioButton
                    Content="{material:PackIcon Kind=FormatLowercase}"
                    IsChecked="{Binding TextCaseChoice, Converter={StaticResource IsEqualConverter}, ConverterParameter={x:Static strings:TextCase.LowerCase}}"
                    Margin="2"
                    Style="{StaticResource MaterialDesignChoiceChipRadioButton}" />
                <RadioButton
                    Content="{material:PackIcon Kind=FormatUppercase}"
                    IsChecked="{Binding TextCaseChoice, Converter={StaticResource IsEqualConverter}, ConverterParameter={x:Static strings:TextCase.UpperCase}}"
                    Margin="2"
                    Style="{StaticResource MaterialDesignChoiceChipRadioButton}" />
                <RadioButton
                    Content="{material:PackIcon Kind=FormatLetterCase}"
                    IsChecked="{Binding TextCaseChoice, Converter={StaticResource IsEqualConverter}, ConverterParameter={x:Static strings:TextCase.TitleCase}}"
                    Margin="2"
                    Style="{StaticResource MaterialDesignChoiceChipRadioButton}" />
            </StackPanel>
        </DockPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type strings:ReplaceStringOperation}">
        <DockPanel>
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding OperationTitle}"
                TextWrapping="Wrap" />

            <material:Card>
                <StackPanel Margin="5">
                    <TextBox
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        material:HintAssist.Hint="Replace:"
                        material:TextFieldAssist.PrefixText="Replace:" />
                    <TextBox
                        Text="{Binding ReplaceText, UpdateSourceTrigger=PropertyChanged}"
                        material:HintAssist.Hint="With:"
                        material:TextFieldAssist.PrefixText="With:" />
                </StackPanel>
            </material:Card>
        </DockPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type strings:SplitStringOperation}">
        <DockPanel>
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding OperationTitle}"
                TextWrapping="Wrap" />

            <material:Card>
                <TextBox Margin="5" Text="{Binding SplitString, UpdateSourceTrigger=PropertyChanged}" />
            </material:Card>
        </DockPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type strings:TrimStringOperation}">
        <DockPanel>
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding OperationTitle}"
                TextWrapping="Wrap" />

            <UniformGrid Columns="5" VerticalAlignment="Center">
                <material:PackIcon
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    Kind="FormatQuoteClose" />
                <material:PackIcon
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    Kind="ArrowRight" />
                <material:PackIcon
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    Kind="Scissors" />
                <material:PackIcon
                    Grid.Column="3"
                    HorizontalAlignment="Center"
                    Kind="ArrowRight" />
                <material:PackIcon
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    Kind="FormatQuoteClose" />
            </UniformGrid>
        </DockPanel>
    </DataTemplate>
</ResourceDictionary>